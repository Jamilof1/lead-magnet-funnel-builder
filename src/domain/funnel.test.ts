import{describe,expect,it}from'vitest';import{buildFunnel}from'./funnel';describe('buildFunnel',()=>{it('creates a complete seven-stage funnel',()=>{expect(buildFunnel({name:'Guide',audience:'Founders',problem:'Manual work',promise:'Save time',format:'PDF',primaryCta:'Book',qualification:'Team size',handoff:'Sales'})).toHaveLength(7)});it('includes a lead conversion event',()=>{expect(buildFunnel({name:'',audience:'',problem:'',promise:'',format:'',primaryCta:'',qualification:'',handoff:''}).some(x=>x.event==='generate_lead')).toBe(true)})});
